# torta

Throwaway visual-direction prototypes for the Iliaora marketing site.

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) and flip variants with the bar at the bottom or `←` `→`. Shareable: `?variant=A` / `B` / `C`.
